import{_ as a,o as t,c as s,ag as n}from"./chunks/framework.ePeAWSvT.js";const h=JSON.parse('{"title":"API 模块","description":"","frontmatter":{},"headers":[],"relativePath":"guide/architecture/api.md","filePath":"guide/architecture/api.md"}'),d={name:"guide/architecture/api.md"};function r(o,e,p,c,l,i){return t(),s("div",null,[...e[0]||(e[0]=[n(`<h1 id="api-模块" tabindex="-1">API 模块 <a class="header-anchor" href="#api-模块" aria-label="Permalink to &quot;API 模块&quot;">​</a></h1><p><code>src/lab/api/</code> — HTTP 路由、外部服务客户端、业务逻辑封装。</p><h2 id="目录结构" tabindex="-1">目录结构 <a class="header-anchor" href="#目录结构" aria-label="Permalink to &quot;目录结构&quot;">​</a></h2><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>api/</span></span>
<span class="line"><span>├── types.py             # 内部类型定义</span></span>
<span class="line"><span>├── routes/              # FastAPI 路由（协议层）</span></span>
<span class="line"><span>│   ├── asr_sherpa.py    # /asr/sherpa — Sherpa-ONNX Paraformer 识别 + VAD</span></span>
<span class="line"><span>│   ├── asr_qwen.py      # /asr/qwen-asr — Qwen3-ASR OpenVINO 识别</span></span>
<span class="line"><span>│   ├── asr_reload.py    # /asr/reload、/asr/sherpa/reload — ASR 热重载</span></span>
<span class="line"><span>│   ├── asr_shared.py    # 共享工具（文件上传暂存、路径处理）</span></span>
<span class="line"><span>│   ├── deeplx.py        # /translate — DeepLX 翻译代理</span></span>
<span class="line"><span>│   ├── faster_qwen_tts.py  # /tts/qwen-tts — Qwen-TTS 合成</span></span>
<span class="line"><span>│   ├── genie_tts.py    # /tts/genie-tts — Genie-TTS 合成</span></span>
<span class="line"><span>│   ├── gsv_lite.py     # /tts/gsv-lite — GSV-Lite 合成</span></span>
<span class="line"><span>│   └── vtuber.py        # /client-ws — VTuber WebSocket 入口</span></span>
<span class="line"><span>├── logic/               # 业务逻辑与模型封装</span></span>
<span class="line"><span>│   ├── sherpa_asr.py    # Sherpa-ONNX 引擎单例管理（加载 / 热重载 / 推理封装）</span></span>
<span class="line"><span>│   ├── qwen_asr.py      # Qwen3-ASR 引擎单例管理（加载 / 热重载 / 推理封装）</span></span>
<span class="line"><span>│   └── faster_qwen_tts.py  # Qwen-TTS 合成逻辑（单次 / 流式）</span></span>
<span class="line"><span>└── clients/             # 外部服务客户端</span></span>
<span class="line"><span>    ├── base_client_interface.py  # BaseRequest / BaseResponse / BaseClientInterface</span></span>
<span class="line"><span>    ├── asr_client.py        # ASRClient</span></span>
<span class="line"><span>    ├── genie_tts_client.py  # GenieTTSClient</span></span>
<span class="line"><span>    ├── gsv_lite_client.py   # GSVLiteClient</span></span>
<span class="line"><span>    ├── deeplx_client.py     # DeepLXClient</span></span>
<span class="line"><span>    ├── reload_client.py     # ReloadClient（模型热重载）</span></span>
<span class="line"><span>    └── vad_client.py        # VADClient</span></span></code></pre></div><h2 id="职责划分" tabindex="-1">职责划分 <a class="header-anchor" href="#职责划分" aria-label="Permalink to &quot;职责划分&quot;">​</a></h2><h3 id="routes-—-协议层" tabindex="-1">Routes — 协议层 <a class="header-anchor" href="#routes-—-协议层" aria-label="Permalink to &quot;Routes — 协议层&quot;">​</a></h3><p>路由只负责 HTTP 协议处理（参数解析、响应格式化），业务逻辑委托给 <code>logic/</code>。</p><table tabindex="0"><thead><tr><th>路由</th><th>前缀</th><th>功能</th></tr></thead><tbody><tr><td><code>asr_sherpa.py</code></td><td><code>/asr/sherpa</code></td><td>Sherpa-ONNX Paraformer 语音识别（transcribe + VAD）</td></tr><tr><td><code>asr_qwen.py</code></td><td><code>/asr/qwen-asr</code></td><td>Qwen3-ASR OpenVINO 识别（0.6B / 1.7B 各自一个端点）</td></tr><tr><td><code>asr_reload.py</code></td><td><code>/asr</code></td><td>ASR 热重载（<code>/reload</code> 全量 + <code>/sherpa/reload</code> 单独）</td></tr><tr><td><code>asr_shared.py</code></td><td>—</td><td>上传文件暂存到缓存目录，<code>file_default</code> 依赖注入</td></tr><tr><td><code>deeplx.py</code></td><td><code>/translate/deeplx</code></td><td>DeepLX 翻译代理（调用外部服务）</td></tr><tr><td><code>llm_translate.py</code></td><td><code>/translate/llm</code></td><td>本地 LLM 翻译（Qwen2.5-0.5B Q8 GGUF，llama-cpp-python）</td></tr><tr><td><code>faster_qwen_tts.py</code></td><td><code>/tts/qwen-tts</code></td><td>Qwen-TTS 语音合成（非流式 + SSE 流式）</td></tr><tr><td><code>genie_tts.py</code></td><td><code>/tts/genie-tts</code></td><td>Genie-TTS 语音合成（POST → WAV）</td></tr><tr><td><code>gsv_lite.py</code></td><td><code>/tts/gsv-lite</code></td><td>GSV-Lite 语音合成（POST → WAV）</td></tr><tr><td><code>vtuber.py</code></td><td><code>/client-ws</code></td><td>Open-LLM-VTuber WebSocket 连接管理</td></tr></tbody></table><h3 id="logic-—-模型封装" tabindex="-1">Logic — 模型封装 <a class="header-anchor" href="#logic-—-模型封装" aria-label="Permalink to &quot;Logic — 模型封装&quot;">​</a></h3><p>每个文件对应一套引擎，同时包含单例管理和推理封装，职责内聚：</p><table tabindex="0"><thead><tr><th>文件</th><th>内容</th></tr></thead><tbody><tr><td><code>logic/sherpa_asr.py</code></td><td><code>load_sherpa_asr</code> / <code>reload_sherpa_asr</code> / <code>sherpa_asr_audio</code> / <code>sherpa_vad_audio</code>。启动时预加载 Paraformer + Silero VAD，热重载先 reset 再重新加载。</td></tr><tr><td><code>logic/qwen_asr.py</code></td><td><code>load_qwen_asr_engine</code> / <code>reload_qwen_asr_engine</code> / <code>qwen_asr_transcribe</code> / <code>preload_configured_qwen_asr_engines</code>。支持多模型并发（0.6B / 1.7B 各自独立单例）。<code>normalize_qwen_model_name</code> 处理路由层传入的各种别名。</td></tr><tr><td><code>logic/faster_qwen_tts.py</code></td><td>Qwen-TTS 合成逻辑（单次 / 流式）</td></tr><tr><td><code>logic/llm_translate.py</code></td><td>LLMTranslateEngine 加载/卸载/路径解析</td></tr><tr><td><code>logic/translate.py</code></td><td><code>TranslateEngineRouter</code>：按 <code>translate_provider</code> 路由到 DeepLX 或本地 LLM</td></tr></tbody></table><p>Sherpa-ONNX 与 Qwen3-ASR 完全解耦，<code>server.py</code> 启动时各自按 <code>[package]</code> flag 独立预加载，互不干扰。</p><h3 id="clients-—-外部服务封装" tabindex="-1">Clients — 外部服务封装 <a class="header-anchor" href="#clients-—-外部服务封装" aria-label="Permalink to &quot;Clients — 外部服务封装&quot;">​</a></h3><p>所有 Client 继承 <code>BaseClientInterface</code>，统一了请求/响应模型和日志。用于从 <code>conversations/</code> 等内部模块调用 TTS、ASR 等服务。</p><h2 id="与其他模块的关系" tabindex="-1">与其他模块的关系 <a class="header-anchor" href="#与其他模块的关系" aria-label="Permalink to &quot;与其他模块的关系&quot;">​</a></h2><ul><li><strong>server.py</strong> 按 <code>[package]</code> 开关挂载路由：<code>sherpa_router</code>、<code>qwen_asr_router</code>、<code>asr_reload_router</code>、<code>deeplx_router</code>、<code>llm_translate_router</code>（<code>llm_translate=true</code>）、<code>qwen_tts_router</code>、<code>vtuber_router</code></li><li><strong>conversations/</strong> 通过 Clients 调用 TTS / 翻译</li><li><strong>agent/</strong> 不直接依赖 api/，而是通过 conversations 层间接使用</li></ul>`,16)])])}const u=a(d,[["render",r]]);export{h as __pageData,u as default};
