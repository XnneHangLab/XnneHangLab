# RoadMap

XnneHangLab 项目的开发路线图与技术债务清理计划。

## 代码清理

### 移除 database 模块

**状态：** 待处理  
**优先级：** 低

`src/lab/database/` 是早期学习 MongoDB 时的练手代码，与主项目无关联，应该移除。

**清理范围：**
- 删除 `src/lab/database/` 目录
- 移除 `server.py` 中的 database routes 挂载
- 清理相关配置和依赖

### 文档清理

**状态：** 待处理  
**优先级：** 中

清理过时或冗余的文档，保持文档体系简洁。

**待清理项：**
- 检查 `docs/guide/` 下是否有过时内容
- 统一文档风格和格式
- 补充缺失的模块文档

## 功能开发

### Memory Bench 集成优化

**状态：** 进行中  
**优先级：** 高

完善 Memory Chat Server 与主项目的集成。

**待完成：**
- [ ] 职责分离重构（透明代理 + 自治 agent）
- [ ] Session 管理 + Tool-loop
- [ ] EMOTION 结构化响应
- [ ] 上下文裁剪策略

### MCP 工具扩展

**状态：** 规划中  
**优先级：** 中

扩展内置工具服务器功能。

**计划：**
- 文件系统操作增强
- 更多网络工具（API 调用、数据抓取）
- 系统信息查询工具

## 技术债务

### CLI 模块定位

**状态：** 待讨论  
**优先级：** 低

`cli.py` 当前只用于 ASR 命令行工具，是否保留需要评估。

**选项：**
1. 保留并扩展为完整的 CLI 工具集
2. 移除，ASR 功能通过 API 调用
3. 拆分到独立的 `lab-cli` 包

---

_本文档持续更新，反映项目最新规划。_
