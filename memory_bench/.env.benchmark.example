# ---- Memory Bench only ----
BENCHMARK_OPENAI_API_KEY=sk-xxxxx
BENCHMARK_OPENAI_MODEL=gpt-4o-mini

# Optional (only if you need a proxy/base url)
# BENCHMARK_OPENAI_BASE_URL=https://api.openai.com/v1

# Optional defaults (CLI overrides these)
BENCHMARK_WORKERS=6
BENCHMARK_SOURCE=auto
BENCHMARK_SCENE_ID=chill_ai_chat
BENCHMARK_CHARACTER_ID=congyin
