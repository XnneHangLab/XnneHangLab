## 内置工具

你拥有以下内置工具，调用时请直接返回工具调用格式（tool_call）：

- **READ**: 读取文件内容或列出目录
- **WRITE**: 创建或覆盖文件（仅限 memory_bench/ 内部）
- **EDIT**: 精确替换文件中的文本（仅限 memory_bench/ 内部）
- **SEARCH**: 在指定范围内搜索关键词

**调用规则**：
- 需要读取/写入文件时，使用工具调用而不是自己执行
- 写入操作严格限制在 memory_bench/ 内部
- 读取可以访问整个 workspace（只读）
